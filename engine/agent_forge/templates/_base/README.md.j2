# {{ agent.name }}

{{ agent.description }}

Generated by **agent_forge** (spec → code) from the Brightspeed agent-ops-console
canonical config. Topology: **{{ topology }}**. Model: **{{ model.primary }}**
via **{{ 'Vertex AI' if vertex else 'Google AI Studio' }}**.

> The generator is deterministic (no LLM used to generate this code). The agent
> itself uses Gemini at runtime.

## Run

```bash
python -m venv .venv && . .venv/Scripts/activate    # (Windows)  or: source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env      # then fill in credentials
{% if rag %}
python -m {{ pkg }}.rag.ingest        # build the local vector index from src/{{ pkg }}/rag/sample_docs
{% endif %}
python -m {{ pkg }}.runtime.cli "your question here"
# or serve an API:
uvicorn {{ pkg }}.runtime.server:app --reload   # POST /converse  ·  GET /health
```

Structural test (no API key needed): `pytest`

## LLM target

{% if vertex %}
Generated for **Vertex AI** (`ChatVertexAI`). Set `GOOGLE_CLOUD_PROJECT` +
application-default credentials. To switch to AI Studio, regenerate with
`--llm-target=aistudio`.
{% else %}
Generated for **Google AI Studio** (`ChatGoogleGenerativeAI`, `GOOGLE_API_KEY`).
The config's `vertex://` prefix is normalized away. To emit real Vertex code
(`ChatVertexAI`), regenerate with `--llm-target=vertex`.
{% endif %}

## Advisory-only scope (LOCKED)

Only read/summarize/draft/recommend/validate tools are bound. Write-capable tools
are emitted **disabled** in `tools.py` (they raise `PermissionError`) and are never
bound to the agent.
{% if disabled_tools %}
Disabled write tools: {% for t in disabled_tools %}`{{ t.name }}`{% if not loop.last %}, {% endif %}{% endfor %}.
Enable one only behind a human-in-the-loop gate after governance approval.
{% endif %}

{% if hitl.process_gates %}
## Human-process gates (not graph interrupts)

These governance gates from the config have no runtime graph node and are **not**
enforced as LangGraph interrupts — handle them in your deployment process:
{% for g in hitl.process_gates %}
- {{ g }}
{% endfor %}
{% endif %}
{% if hitl.interrupt_before or hitl.tool_call_interrupt %}
## Runtime HITL (graph interrupts)

{% if hitl.interrupt_before %}The graph interrupts before: {% for n in hitl.interrupt_before %}`{{ n }}`{% if not loop.last %}, {% endif %}{% endfor %}.{% endif %}
{% if hitl.tool_call_interrupt %}Every tool call is interrupted for human approval (Critical path).{% endif %}
Resume with the LangGraph checkpointer + `Command(resume=…)`.
{% endif %}

{% if rag %}
## Knowledge / RAG

Retrieval uses a pure-Python **InMemoryVectorStore** (`retriever.py`, top_k={{ rag.top_k }},
score_threshold={{ rag.score_threshold }}) built lazily from the bundled sample docs
in `src/{{ pkg }}/rag/sample_docs/` — no native deps, runs out of the box. Referenced
sources: {{ rag.knowledge_source_refs | join(', ') or '(none)' }}. For a persistent/managed
store, swap `retriever.py` for Chroma or AlloyDB/pgvector (see `index_target`
`{{ rag.index_target }}`).
{% endif %}

{% if blockers.has_hard_blockers %}
## ⚠ Build blockers

Review before deploying (write tools are never bound regardless):
{% if blockers.write_tools %}
- write tools bound (advisory violation): {{ blockers.write_tools | join(', ') }}
{% endif %}
{% if blockers.required_config_unresolved %}
- required config unresolved: {{ blockers.required_config_unresolved | join(', ') }}
{% endif %}
{% if blockers.unreviewed_write_adjacent %}
- write-adjacent config with no review_card — review in the console first
{% endif %}
{% endif %}

The exact source config is embedded at `config/agent.config.json` (provenance).

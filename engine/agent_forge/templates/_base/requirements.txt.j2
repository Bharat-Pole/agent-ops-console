# Pinned for reproducibility (agent_forge is deterministic). The engine's smoke
# test proves this set compiles the generated graph. langchain-google-genai >=4
# is REQUIRED: Gemini 3 models demand thought_signature echo on tool calls, which
# older (google-generativeai-based) versions drop → 400s on the second turn.
langgraph>=1.0,<2.0
langgraph-prebuilt>=1.0,<2.0
langchain-core>=1.0,<2.0
{% if vertex %}
langchain-google-vertexai>=3.0.0,<5.0.0
{% else %}
langchain-google-genai>=4.3.0,<5.0.0
{% endif %}
{% if needs_ddgs %}
ddgs>=6.0.0
{% endif %}
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.5.0

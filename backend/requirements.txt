fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
bcrypt>=4.1
itsdangerous>=2.1
pydantic-settings>=2.2
httpx>=0.27
pytest>=8.0
# Postgres driver — used when DATABASE_URL points at postgres (compose/CI);
# local tests default to SQLite via SQLAlchemy variants.
psycopg[binary]>=3.1; platform_system != "emscripten"
# LLM provider (Gemini primary per the Blueprint; adapter seam in app/adapters)
google-genai>=1.0
# Asset Studio (Increment C): secrets-at-rest encryption + PDF parsing + MCP
cryptography>=42
pypdf>=4.0
mcp>=1.0
python-multipart>=0.0.9
# Execution engine substrate (Decision 5.1): LangGraph + durable checkpointers
langgraph>=0.4
langgraph-checkpoint-sqlite>=2.0
langgraph-checkpoint-postgres>=2.0; platform_system != "emscripten"

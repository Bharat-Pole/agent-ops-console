# Alembic config. The database URL is NOT set here — migrations/env.py resolves
# it from app settings (PLATFORM_DATABASE_URL), the same seam the app uses, so
# app and migrations can never point at different databases by accident.
[alembic]
script_location = migrations
prepend_sys_path = .

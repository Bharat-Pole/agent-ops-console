node_modules
dist
dist-ssr
*.local
.vite
node_modules/.tmp
__pycache__
*.pyc
backend/.venv

# internal — not published. These quote the SOW and carry commercial detail.
# CLAUDE.md is deliberately NOT in this list: it is the handoff doc and ships
# redacted (no key status, no auth/CORS detail, no local paths).
CONCERNS.md
DEMO_TOOLS_MCP.md


# server env / secrets
.env

# client commercial documents — NEVER commit these, the repo is public
*.docx
*.pptx
*.pdf
*.xlsx
